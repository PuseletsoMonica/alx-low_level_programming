#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 *
 * Return: 1 if c is uppercase
 */
int main(void)
{char c;
	if c = 'A';
	printf("%c: %d\n", c, _isupper(c));
	else if c = 'a';
	printf("%c: %d\n", c, _isupper(c));
	return (0);
}
