#include <stdio.h>
/**
 * main - print all single digits of base 10
 * Return: Always 0 (Success)
 */
int main(void)
	{int num;
		for (num = 0; < 10; num++)
		putchar(("i % 10) + "0");

		putchar ("\n");
		return (0);
	}
